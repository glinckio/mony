# Requirements — <Feature Name>

## Summary

One paragraph: what this feature does and why it exists.

## User stories

- As a `<role>`, I want `<capability>`, so that `<benefit>`.

## Acceptance criteria (EARS format)

- WHEN `<trigger>`, THE SYSTEM SHALL `<expected behavior>`.
- IF `<precondition>`, THEN THE SYSTEM SHALL `<expected behavior>`.

## Out of scope

- Explicitly list what this feature does NOT cover, to prevent scope creep.

## Open questions

- List anything blocking implementation that needs a decision.
